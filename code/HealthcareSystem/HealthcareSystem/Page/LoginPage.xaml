<page:BasePage
    x:Class="HealthcareSystem.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HealthcareSystem"
    xmlns:page="using:HealthcareSystem.Page">

    <Grid Width="500" Height="250" MinWidth="500" MinHeight="250" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="10">
            <TextBlock Text="Login" FontSize="24" />
            <TextBox x:Name="UsernameTextBox" PlaceholderText="Username" Margin="0,10,0,0" KeyDown="InputField_KeyDown"/>
            <PasswordBox x:Name="PasswordBox" PlaceholderText="Password" Margin="0,10,0,0" KeyDown="InputField_KeyDown"/>
            <Button Content="Login" Click="LoginButton_Click" Margin="0,20,0,0"/>
            <TextBlock x:Name="LoginError" Foreground="Red" />
        </StackPanel>
    </Grid>
</page:BasePage>
