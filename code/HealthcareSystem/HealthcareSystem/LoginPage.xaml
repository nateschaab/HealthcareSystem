<Page
    x:Class="HealthcareSystem.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YourApp">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20">
            <TextBlock Text="Login" FontSize="24" />
            <TextBox x:Name="UsernameTextBox" PlaceholderText="Username" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" PlaceholderText="Password" Margin="0,10,0,0"/>
            <Button Content="Login" Click="LoginButton_Click" Margin="0,20,0,0"/>
            <TextBlock x:Name="LoginError" Foreground="Red" />
        </StackPanel>
    </Grid>
</Page>
